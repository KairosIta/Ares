"""La REPL di Ares.

``chat`` avvia e coordina la sessione; ``commands`` contiene la tabella dei
comandi locali e il loro dispatch; ``render`` presenta eventi, conferme e
metriche del turno; ``editor`` gestisce prompt, completamento, input
multilinea e cronologia privata; ``ui`` rende streaming Markdown, pannelli e
tabelle con Rich.
"""
